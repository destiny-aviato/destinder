$("div#<%= @micropost.id %>").slideUp();
$('.row.posts').html("<%= j (render @microposts) %>");
$('ul.tabs').tabs();
Materialize.toast('Post Removed', 4000)
$('.tooltipped').tooltip({ 
    delay: 50,
    html: true
 });
 $('.tooltip').tooltipster({
    theme: ['tooltipster-noir', 'tooltipster-noir-customized'],
    delay: 50
});
$('.scroller-test').slick({
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    slidesToScroll: 1,
    arrows: true       
});

$('.modal').modal();

$('.scroller-test').on('swipe', function(event, slick, direction){
    console.log(direction);
    // left
    $('ul.tabs').tabs();
  });

  $('.game_type_select').change(function() {
    selection = $(this).val();
    // console.log(selection);
    $("#game-type-filter").val(selection);
    if (selection == "Trials of Osiris" || selection == "Trials of the Nine") {
        $('#filter-sliders').show();
    } else {
        $('#filter-sliders').hide();
    }
    // $('#new-post-modal').modal('close');
    // $("#filter_game_form").submit();
});