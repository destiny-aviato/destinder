$('.row.posts').html("<%= j (render @microposts) %>");
// $('.row.posts').prepend("<%= j render partial: 'micropost', locals: {micropost: @micropost} %>");
$('ul.tabs').tabs();
$('.collapsible').collapsible('close', 0);
Materialize.toast('Post Created!', 4000)
$('.tooltipped').tooltip({ 
    delay: 50,
    html: true
 });
 $('.tooltip').tooltipster({
    theme: ['tooltipster-noir', 'tooltipster-noir-customized'],
    delay: 50
});
$('.scroller-test').slick({
    dots: true,
    infinite: true,
    speed: 300,
    slidesToShow: 1,
    slidesToScroll: 1,
    arrows: true       
});

$('.scroller-test').on('swipe', function(event, slick, direction){
    console.log(direction);
    // left
    $('ul.tabs').tabs();
  });